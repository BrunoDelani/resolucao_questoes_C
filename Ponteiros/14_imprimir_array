#include <stdio.h>
#include <stdlib.h>
void ler(char *v,int size){
    int i;
    for(i = 0; i <size;i++){
    printf("%c",v[i]);
    }
}


int main()
{
    char *v;
    v = malloc (100*sizeof(char));
    scanf("%s",v);
    ler(v,100);
    return 0;
}
