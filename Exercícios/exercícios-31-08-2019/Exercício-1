#include <stdio.h>

int main()
{
    char frase[100];
    int i = 0;
    
    
    scanf("%[^\n]%*c",frase);

    
    while(frase[i] != '\0'){
        if(frase[i] != ' '){
            printf("%c",frase[i]);
        }
        i++;
    }

    return 0;
}
